diff --git a/recompinput/src/input_events.cpp b/recompinput/src/input_events.cpp
index 16a5f0d..b4292a9 100644
--- a/recompinput/src/input_events.cpp
+++ b/recompinput/src/input_events.cpp
@@ -10,6 +10,11 @@
 #include <TargetConditionals.h>
 #endif

+#if defined(__APPLE__) && TARGET_OS_IPHONE
+extern "C" void BanjoPadTouch_ReleaseAll(void);
+extern "C" void BanjoPadTouch_SetControllerConnected(int connected);
+#endif
+
 static struct {
     std::list<std::filesystem::path> files_dropped;
 } DropState;
@@ -94 +99,5 @@ bool sdl_event_filter(void* userdata, SDL_Event* event) {
+#if defined(__APPLE__) && TARGET_OS_IPHONE
+        BanjoPadTouch_SetControllerConnected(1);
+#endif
+
         ControllerGUID guid = profiles::get_guid_from_sdl_controller(controller);
@@ -107,6 +115,9 @@ bool sdl_event_filter(void* userdata, SDL_Event* event) {
         SDL_ControllerDeviceEvent* controller_event = &event->cdevice;
         printf("Controller removed: %d\n", controller_event->which);
         recompinput::remove_controller_state(controller_event->which);
+#if defined(__APPLE__) && TARGET_OS_IPHONE
+        BanjoPadTouch_SetControllerConnected(SDL_NumJoysticks() > 0 ? 1 : 0);
+#endif
     }
     break;
     case SDL_EventType::SDL_QUIT: {
@@ -240,6 +251,9 @@ bool sdl_event_filter(void* userdata, SDL_Event* event) {
         recompui::drop_files(DropState.files_dropped);
         break;
 #if defined(__APPLE__) && TARGET_OS_IPHONE
+    case SDL_EventType::SDL_APP_WILLENTERBACKGROUND:
+        BanjoPadTouch_ReleaseAll();
+        break;
     case SDL_EventType::SDL_APP_DIDENTERFOREGROUND:
         if (!ultramodern::is_game_started()) {
             recompui::update_mod_list(true);
diff --git a/recompui/src/base/ui_state.cpp b/recompui/src/base/ui_state.cpp
index 277a421..e842929 100644
--- a/recompui/src/base/ui_state.cpp
+++ b/recompui/src/base/ui_state.cpp
@@ -33,6 +33,14 @@
 #include "rml_elements/ui_rml_elements.h"
 #include "util/file.h"

+#if defined(__APPLE__)
+#include <TargetConditionals.h>
+#endif
+
+#if defined(__APPLE__) && TARGET_OS_IPHONE
+extern "C" void BanjoPadTouch_SetMenuVisible(int visible);
+#endif
+
 static std::atomic_bool cursor_enabled = true;

 void recompui::open_quit_game_prompt() {
@@ -650,6 +658,11 @@ void draw_hook(plume::RenderCommandList* command_list, plume::RenderFramebuffer*

     apply_background_input_mode();

+#if defined(__APPLE__) && TARGET_OS_IPHONE
+    BanjoPadTouch_SetMenuVisible(
+        !ultramodern::is_game_started() || recompui::is_any_context_shown());
+#endif
+
     // Return early if the ui context has been destroyed already.
     if (!ui_state) {
         return;
diff --git a/recompui/src/config/ui_config_page_controls.cpp b/recompui/src/config/ui_config_page_controls.cpp
index f379a6f..b53e44e 100644
--- a/recompui/src/config/ui_config_page_controls.cpp
+++ b/recompui/src/config/ui_config_page_controls.cpp
@@ -9,6 +9,23 @@
 #include "recompinput/profiles.h"
 #include "recompui/config.h"
+#include "librecomp/game.hpp"

+#if defined(__APPLE__)
+#include <TargetConditionals.h>
+#endif
+
+#if defined(__APPLE__) && TARGET_OS_IPHONE
+extern "C" int BanjoPadTouch_Enabled(void);
+extern "C" int BanjoPadTouch_HideWhenControllerConnected(void);
+extern "C" int BanjoPadTouch_ShowDpad(void);
+extern "C" int BanjoPadTouch_ShowL(void);
+extern "C" void BanjoPadTouch_BeginLayoutEditing(void);
+extern "C" void BanjoPadTouch_SetEnabled(int enabled);
+extern "C" void BanjoPadTouch_SetHideWhenControllerConnected(int hidden);
+extern "C" void BanjoPadTouch_SetShowDpad(int visible);
+extern "C" void BanjoPadTouch_SetShowL(int visible);
+#endif
+
 namespace recompui {

 ConfigPageControls *controls_page = nullptr;
@@ -518,6 +535,61 @@ void ConfigPageControls::render_control_mappings() {
         body_left_scroll->set_padding_right(8.0f);
         body_left_scroll->set_debug_id("Mappings Scroll Container");

+#if defined(__APPLE__) && TARGET_OS_IPHONE
+        auto create_touch_toggle = [&](const char *label, bool checked) {
+            auto row = context.create_element<Element>(body_left_scroll, 0, "div", false);
+            row->set_display(Display::Flex);
+            row->set_flex_direction(FlexDirection::Row);
+            row->set_align_items(AlignItems::Center);
+            row->set_justify_content(JustifyContent::SpaceBetween);
+            row->set_width(100.0f, Unit::Percent);
+            row->set_padding(16.0f);
+            row->set_as_navigation_container(NavigationType::GridRow);
+            context.create_element<Label>(row, label, theme::Typography::LabelMD);
+            auto toggle = context.create_element<Toggle>(row, ToggleSize::Medium);
+            toggle->set_checked(checked);
+            return toggle;
+        };
+        auto touch_controls_toggle =
+            create_touch_toggle("Touch Controls", BanjoPadTouch_Enabled() != 0);
+        auto show_l_toggle =
+            create_touch_toggle("Show L Button", BanjoPadTouch_ShowL() != 0);
+        auto show_dpad_toggle =
+            create_touch_toggle("Show D-pad", BanjoPadTouch_ShowDpad() != 0);
+        auto hide_with_controller_toggle = create_touch_toggle(
+            "Hide When Controller Connected",
+            BanjoPadTouch_HideWhenControllerConnected() != 0);
+        auto customize_touch_button = context.create_element<Button>(
+            body_left_scroll,
+            "Customize Touch Layout",
+            ButtonStyle::Secondary,
+            ButtonSize::Medium);
+        customize_touch_button->set_width(100.0f, Unit::Percent);
+        customize_touch_button->set_margin_top(8.0f);
+        customize_touch_button->set_margin_bottom(16.0f);
+        const bool game_started = ultramodern::is_game_started();
+        customize_touch_button->set_enabled(
+            BanjoPadTouch_Enabled() != 0 && game_started);
+        customize_touch_button->add_pressed_callback([]() {
+            BanjoPadTouch_BeginLayoutEditing();
+        });
+
+        touch_controls_toggle->add_checked_callback(
+            [customize_touch_button, game_started](bool checked) {
+                BanjoPadTouch_SetEnabled(checked ? 1 : 0);
+                customize_touch_button->set_enabled(checked && game_started);
+            });
+        show_l_toggle->add_checked_callback([](bool checked) {
+            BanjoPadTouch_SetShowL(checked ? 1 : 0);
+        });
+        show_dpad_toggle->add_checked_callback([](bool checked) {
+            BanjoPadTouch_SetShowDpad(checked ? 1 : 0);
+        });
+        hide_with_controller_toggle->add_checked_callback([](bool checked) {
+            BanjoPadTouch_SetHideWhenControllerConnected(checked ? 1 : 0);
+        });
+#endif
+
         game_input_rows.clear();

         create_game_input_contexts();
